#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int sys_m;
    int sys_n;
    int liczba_m;
    int liczba_n

    cout << "Podaj system liczbowy(liczba): " << endl;
    cin << sys_n;
    cout << "Podaj liczbe w systemie: " << n  << "-ym" << endl;
    cin >> liczba_n;
    cout << "Na jaki system konwertujemy(liczba): " << endl;

    for (int i=strlen(liczba_n)-1; i>=0; i--) {
        if (liczba_n[i] >='0' && liczba_n[i] <='9')

    }
            DOKONCZ!!!!!!!!!!!!!!

        return 0;
    }
